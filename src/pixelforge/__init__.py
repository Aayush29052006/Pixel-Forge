"""Pixel Forge — batch image format conversion, resizing, and rotation.

Core package. Contains the pure conversion engine (core.py), the batch
runner that walks a set of files and applies it per-file (batch.py),
and a Flask web app (webapp/) as the single interface on top.
"""
